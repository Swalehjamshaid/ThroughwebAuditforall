[build]
builder = "docker"

[deploy]
restartPolicyType = "ON_FAILURE"
# This override ensures the shell expands $PORT into a number
startCommand = "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"

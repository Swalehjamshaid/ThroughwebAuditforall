
# railway.toml
[build]
builder = "RAILPACK"
root = "/File"                          # Only if using Option A
install = "pip install -r requirements.txt"
build   = ""
start   = "gunicorn ffteck_audit.app:app --workers 2 --threads 4 --bind 0.0.0.0:$PORT"

[variables]
PORT = "8000"

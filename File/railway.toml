
# railway.toml
[service]
name = "my-app"
startCommand = "gunicorn app:app --bind 0.0.0.0:$PORT"  # For Flask/Django; adjust for your framework
healthcheckPath = "/health"
healthcheckTimeout = 180  # Increase timeout for slow startups

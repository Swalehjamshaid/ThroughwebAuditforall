
[build]
builder = "RAILPACK"

[deploy]
startCommand = "python -m uvicorn fftech_audit.app:app --host 0.0.0.0 --port ${PORT} --log-level info"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
``

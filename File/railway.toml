
# railway.toml

[deploy]
startCommand = "uvicorn fftech_audit.app:app --host 0.0.0.0 --port $PORT"

[env]
ENABLE_LIGHTHOUSE = "0"
MAX_PAGES_MAIN = "120"
MAX_PAGES_COMP = "40"
CRAWL_TIMEOUT = "15.0"
MAX_LINK_CHECKS = "150"
ACCEPT_LANGUAGE = "en-US,en;q=0.8"

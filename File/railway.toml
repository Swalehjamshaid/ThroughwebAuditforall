
[build]
builder = "RAILPACK"

[deploy]
startCommand = "uvicorn fftech_audit.app:app --host 0.0.0.0 --port ${PORT}"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"

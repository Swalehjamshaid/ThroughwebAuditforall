
[build]
builder = "RAILPACK"
root    = "/File"

install = "pip install -r requirements.txt"
build   = ""
start   = "uvicorn fftech_audit.app:app --host 0.0.0.0 --port $PORT"

[variables]
PORT = "8000"

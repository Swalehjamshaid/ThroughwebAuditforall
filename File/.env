
# ---------- Runtime ----------
ENV=production
DEBUG=false

# Public URL (match your Railway domain exactly; keep HTTPS)
APP_BASE_URL=https://<your-service-name>.up.railway.app

# ---------- Security ----------
# Generate strong random secrets (32-64 chars)
SESSION_SECRET=Zp4eYkOltKQHn1C9dV5Jwq7rSGt2xF8eKf3mNb6pLc9rUd2Wq3
TOKEN_SECRET=HWq9eT8mC2bL5rA7pZ0nY3kU6vF1xD4sG8tM0cN2qP5eR7uL9

# ---------- Database ----------
# If you have Railway Postgres, use the provided URL.
# Prefer pg8000 driver to avoid psycopg2 ABI issues on Python 3.13
DATABASE_URL=postgresql+pg8000://USER:PASSWORD@HOST:PORT/DBNAME
# If you do NOT use a DB yet, either remove this line or set:
# DATABASE_URL=sqlite:///./data.db
# and ensure your code conditionally initializes the DB client.

# ---------- App-specific ----------
# Keep crawl heavy tasks off startup; do them on-demand
MAX_PAGES=60  # reduce default (override per request if needed)

# Optional: Gunicorn tuning via env (used in railway.toml command)
WEB_CONCURRENCY=2
WEB_TIMEOUT=300

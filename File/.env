
# -----------------------------
# FF Tech AI • Local .env
# -----------------------------
# Environment flags
ENV=development
DEBUG=true

# Uvicorn default bindings (used by Docker/CLI, not directly in code)
HOST=0.0.0.0
PORT=8000

# Base URL for magic link emails (local)
APP_BASE_URL=http://localhost:8000

# Security secrets (generate strong randoms)
# macOS/Linux: openssl rand -hex 32
# Windows (PowerShell): [guid]::NewGuid().ToString("N")
SESSION_SECRET=replace_with_random_64_hex_chars
TOKEN_SECRET=replace_with_random_64_hex_chars

# Database:
# Leave DATABASE_URL empty to use local SQLite (fftech_audit/audit.sqlite3)
# For local Postgres, use pg8000 (Python 3.13 compatible):
# DATABASE_URL=postgresql+pg8000://USER:PASSWORD@HOST:5432/DBNAME
DATABASE_URL=

# Crawl settings
MAX_PAGES=120

# nixpacks.toml — place this file at the ROOT of your repository (not inside File/)

[phases.setup]
nixpkgsArchive = "f9069e1c0155cc6d7d0e996f85f16c26e11be800"  # stable Nixpkgs

[phases.build]
# Tell Railway to use the Dockerfile inside the File/ subdirectory
dockerfile = "File/Dockerfile"

# Optional: explicitly set the start command (your Dockerfile already has this, so not required)
# cmds = ["uvicorn fftech_audit.app:app --host 0.0.0.0 --port $PORT"]

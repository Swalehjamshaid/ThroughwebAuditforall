
{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "startCommand": "gunicorn app.main:app -b 0.0.0.0:$PORT -w 2"
  }
}

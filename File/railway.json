
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    // Path to your Dockerfile (relative to repo root)
    "dockerfilePath": "File/Dockerfile",

    // Build context. Use "." if your app sources are at the repo root,
    // or "File" if everything needed for the build lives in the File/ folder.
    "context": ".",

    // Optional: your build args here if needed
    "buildArgs": {}
  },
  "deploy": {
    // Optional: command to run after container starts (adjust as needed)
    // "startCommand": "node server.js"
  }
}
``

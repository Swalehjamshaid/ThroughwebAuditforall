
[build]
builder = "RAILPACK"
install = "pip install -r requirements.txt"
build   = ""
start   = "uvicorn main:app --host 0.0.0.0 --port $PORT"   # Flask: "gunicorn main:app"

[variables]
PORT = "8000"


PYTHONDONTWRITEBYTECODE="1"
PYTHONUNBUFFERED="1"

DATABASE_URL="${{Postgres.DATABASE_URL}}"

UI_BRAND_NAME="FF Tech"
BASE_URL="https://throughwebauditforall-production-up.railway.app"

JWT_SECRET="pvop2w6yluotcim1v36n3kd8adkcg5f3"
JWT_ALGO="HS256"
AUTH_SALT="fftech_salt"
AUTH_ITERATIONS="200000"

GOOGLE_PSI_API_KEY="AIzaSyDUVptDEm1ZbiBdb5m1DGjvKCW_LBVJMEw"

# --- SMTP / Email settings ---
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="roy.jamshaid@gmail.com"
SMTP_PASSWORD="REAL_16_CHAR_APP_PASSWORD"

# --- New: Email feature flag & protections ---
# Disable emails cleanly if your container network blocks SMTP
MAGIC_EMAIL_ENABLED="false"      # set "true" only if smtp.gmail.com:587 is reachable

# Prevent hangs with timeouts & bounded retries
SMTP_TIMEOUT_SEC="6.0"           # socket/connect timeout in seconds
SMTP_MAX_RETRIES="2"             # total attempts = 1 (initial) + retries
SMTP_BACKOFF_BASE_SEC="1.0"      # exponential backoff base (1s, then 2s)

# Optional: logging level
LOG_LEVEL="INFO"


# Python runtime settings
PYTHONDONTWRITEBYTECODE="1"
PYTHONUNBUFFERED="1"

# Database
DATABASE_URL="${{Postgres.DATABASE_URL}}"

# App branding & base URL
UI_BRAND_NAME="FF Tech"
BASE_URL="https://throughwebauditforall-production.up.railway.app"

# JWT & Auth
JWT_SECRET="pvop2w6yluotcim1v36n3kd8adkcg5f3"
JWT_ALGO="HS256"
AUTH_SALT="fftech_salt"
AUTH_ITERATIONS="200000"

# Google PSI API Key
GOOGLE_PSI_API_KEY="AIzaSyDUVptDEm1ZbiBdb5m1DGjvKCW_LBVJMEw"

# --- SMTP / Email settings ---
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="roy.jamshaid@gmail.com"
SMTP_PASSWORD="mbck adxn depm ehjl"   # Gmail App Password (16 chars)

# ✅ Permanent fix for SMTP hang in containers:
# Disable email sending if outbound SMTP is blocked (Railway often blocks it)
MAGIC_EMAIL_ENABLED="false"          # Set to "true" ONLY if SMTP works

# ✅ Add safe timeouts & retries to prevent blocking
SMTP_TIMEOUT_SEC="6.0"               # Socket timeout for SMTP connection
SMTP_MAX_RETRIES="2"                 # Bounded retries (total attempts = 1 + retries)
SMTP_BACKOFF_BASE_SEC="1.0"          # Exponential backoff base delay

# Logging
LOG_LEVEL="INFO"

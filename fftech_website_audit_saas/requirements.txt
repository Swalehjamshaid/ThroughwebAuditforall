# ── Core API and Server ──────────────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.32.1
uvloop==0.21.0
python-multipart==0.0.9

# ── Configuration and Validation ─────────────────────────────────────────────────
pydantic==2.10.4
pydantic-core==2.27.2
pydantic-settings==2.7.1
python-dotenv==1.0.1
email-validator==2.2.0

# ── Database ─────────────────────────────────────────────────────────────────────
SQLAlchemy==2.0.36
psycopg[binary]==3.2.4
alembic==1.13.3               # Added: Database migrations (highly recommended)

# ── Authentication and Security ──────────────────────────────────────────────────
PyJWT==2.10.1
itsdangerous==2.2.0
bcrypt==4.2.1
passlib[bcrypt]==1.7.4        # Added: Better password hashing utilities

# ── Web Scraping, HTTP Clients & Crawling ────────────────────────────────────────
httpx==0.28.1
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0
playwright==1.50.0            # Added: For JavaScript rendering + mobile emulation
scrapy==2.12.0                # Added: Powerful crawler for deep metrics (200+)

# ── Automated Scheduling & Background Tasks ──────────────────────────────────────
apscheduler==3.11.0
celery==5.4.0                 # Added: Robust background tasks (scheduled audits)
redis==5.2.0                  # Added: Celery broker/backend (Railway supports Redis)

# ── Data Analysis and Visualization ──────────────────────────────────────────────
numpy==2.1.3
pandas==2.2.3
matplotlib==3.9.2
seaborn==0.13.2               # Added: Better charts for reports

# ── Reporting and Exports (PDF, CSV, PPTX) ───────────────────────────────────────
reportlab==4.2.5
openpyxl==3.1.5
python-pptx==1.0.2
Jinja2==3.1.4
weasyprint==62.3              # Added: Modern HTML → PDF (best for 5-page reports)
pypdf==5.1.0                  # Added: PDF manipulation if needed

# ── Email Delivery ───────────────────────────────────────────────────────────────
resend==2.2.0

# ── Optional / Future-proof (add when needed) ───────────────────────────────────
# slowapi==0.1.9              # Rate limiting (prevent abuse on open audits)
# python-dotenv[cli]==1.0.1   # For CLI env management
# python-multipart==0.0.9     # Already present


# Core framework
fastapi>=0.110
uvicorn[standard]>=0.23

# Templates & static files
Jinja2>=3.1
aiofiles>=23.1  # recommended for serving static files

# Forms (FastAPI Form(...) requires this)
python-multipart>=0.0.6

# Database & driver (SQLAlchemy 2.x + Postgres v3 driver)
SQLAlchemy>=2.0
psycopg[binary]>=3.1  # Railway Postgres; matches "postgresql+psycopg" dialect

# Auth & security
passlib[bcrypt]>=1.7.4
PyJWT>=2.8

# HTTP fetching & parsing for audits
requests>=2.31
beautifulsoup4>=4.12
lxml>=4.9   # optional but improves HTML parsing robustness

# PDF report
reportlab>=4.0

# Optional QoL (for local dev)
python-dotenv>=1.0  # load .env locally if you like

# If you use Python < 3.9 locally, you’ll need:
# backports.zoneinfo>=0.2.1

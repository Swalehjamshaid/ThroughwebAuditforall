[build]
builder = "docker"

[deploy]
restartPolicyType = "ON_FAILURE"
# This override ensures the port is injected correctly by Railway
startCommand = "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"

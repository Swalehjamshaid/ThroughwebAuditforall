[build]
builder = "docker"

[deploy]
restartPolicyType = "ON_FAILURE"
# This tells Railway to use the actual port number instead of the text "$PORT"
startCommand = "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"

[build]
builder = "docker"

[deploy]
restartPolicyType = "ON_FAILURE"
# This override tells Railway how to launch your FastAPI app
startCommand = "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"

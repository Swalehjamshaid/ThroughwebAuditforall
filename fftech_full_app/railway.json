{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port $PORT"
    // or python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT
  }
}
